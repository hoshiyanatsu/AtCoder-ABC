#include <iostream>
#include <string>

using namespace std;

int main()
{
    int n, q;
    vector<int> ev(n);
    cin >> n >> q;
    for (int i = 0; i < q; i++){
        cin >> ev.at(i);
    }

    for (int i = 0; i < q; i++){
        if (ev.at(i) == '1'){
            
        }
    }
}